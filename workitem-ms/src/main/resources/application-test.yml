server:
  port: 7005

spring:
  application:
    name: workitem-ms

  datasource:
    url: jdbc:mysql://localhost:3306/freight_transport_region_workitems
    username: root
    password: 9398434528
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mvc:
    format:
      date: dd-MMM-yyyy

  messages:
    basename: ValidationMessages
