server:
  port: 8761

spring:
  application:
    name: eureka-server-serviceRegistry

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false


